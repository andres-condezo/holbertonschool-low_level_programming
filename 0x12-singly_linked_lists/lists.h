#ifndef VARIADIC_FNCTIONS
#define VARIADIC_FNCTIONS
#include <stdio.h>
#include <stdlib.h>

int _putchar(char c);
size_t print_list(const list_t *h);


struct Node {
    int data;
    struct Node* next;
};

#endif

#include "holberton.h"
/**
 * _isupper - checks for uppercase character.
 * @c: Character to be checked.
 *
 * Return: 0 success.
 */
int _isupper(int c)
{

	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}

return (0);
}
